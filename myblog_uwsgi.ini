[uwsgi]
 
    chdir           = /root/lanvce/myblog/
    module          = myblog.wsgi
    master          = true
    processes       = 4
    # the socket (use the full path to be safe
    socket          = 0.0.0.0:8000
    # ... with appropriate permissions - may be needed
    # chmod-socket    = 664
    # clear environment on exit
    vacuum          = true
  
 
